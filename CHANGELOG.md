# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
版本号对应 [docs/04-roadmap.md](./docs/04-roadmap.md) 里的里程碑。

## [0.25.1] · 对话页三个 bug

用户报了三条，其中两条是同一个根因。

### 「只能发给 tst1」和「下拉框切了不生效」是同一个 bug

点一段对话时，代码取「不是 cli 的那一个」当收件人 —— 可 `tst1 ↔ tst2` 这种
**「对方」有两个**，永远取到第一个。而人为了「接着发送」总要先点一下，
于是每次都把下拉框里选的 tst2 覆盖回 tst1。

现在：**已经选的人只要在这段对话里就不动它**，只有当前收件人跟这段无关时才替你
挑（那多半是你刚点了另一段）。换下拉框也同理 —— 换成这段里的另一位是
「还在这段里，改成跟他说」，换成无关的人才是开一段新的。

收件人还写到了选中那段的标记上（`接着这段说 → bb`）—— 发给谁不该靠猜。

### 看不到 Agent 回给 cli 的话

往来只读 `inbox/done/`，而归档是「处理完」才发生的。`cli` **按设计就没有处理者**
（它只是 `anthill send` 用来收回执和结果的信箱，从来没有 agentd 跑它），
于是别人回给你的话永远停在 `new/` —— 「它回了」和「页面上什么都没有」同时成立。

现在 `new/`、`cur/`、`done/` 都读。收件箱里躺着的一样是**已经送到**的消息。
（`cur/` 与 `done/` 交叠的瞬间按信封 id 去重，不会出现两遍。）

## [0.25.0] · 对话合成一页；起来就在值守；等待不再有终点

用户三条反馈，都指向同一件事：**能用**和**顺手**之间还差着几步。

### 「Agent 往来」并进「对话」

上一版把它做成了单独一页 —— 而它和「对话」本来就是同一种东西
（一条 thread 上的一串消息），按「谁参与了」把它劈成两页，
是把实现细节摆到了人脸上。

现在「对话」就是全部：你跟 Agent 说的、Agent 之间说的，一处看。
合起来还白捡一件事：**点一段 Agent 之间的对话就能插进去说一句** ——
「人随时能插进正在进行的协作里」本来就是桥接设计里写着的目标，
现在页面上真做得到了（实测三方同 thread）。

顺带补了个空档：往来读的是收件方的归档，所以一条消息要等对方处理完才看得见 ——
对方**没启动**时那个空档是永远的，页面上看着就像消息丢了。现在拿本机记的发件兜住。

### 起来就在值守，不用先跟它说话

`ANTHILL_AGENT=tst1 claude` 起来之后还得你先说一句它才开始听。
根因是 Claude Code 回合制：**没有用户回合，模型不会动** ——
所以 hook 也好、MCP 也好，都只能等你开口。

唯一能在启动那一刻推动它的，是把提示词当作首个提问传进去：

```bash
ANTHILL_AGENT=cc1 claude "$(anthill bridge cc1 --prompt -w /工作区)"
```

新增 `anthill bridge --prompt`。提示词现取而不是内联 —— 内联的话这段话每改一次，
先前粘出去的命令就旧一次。

### 等待不再有终点

`--wait -1` = 永不超时。原来 `--wait 600` 每十分钟必醒一次：没消息也要烧掉一轮
对话去发现「没事」，再挂一条。值守本来就该一直挂着，有消息才醒。
（`--wait 0` 仍是「看一眼就走」，hook 那条路靠的就是这个语义，没动。）

### 顺带

- `watch_prompt` / `connect_recipes` / `anthill_exe` 从 `web/bridge_panel.py`
  搬到 `adapters/bridge_connect.py` —— CLI 要用同一段提示词，而让 `cli/`
  反过来 import `web/` 是错的方向。

## [0.24.0] · 「Agent 往来」：它们之间聊了什么

面板上原来有三种「看见」，没有一种能回答**「tst1 和 tst2 到底聊了什么」**：
消息流有元数据没正文，对话页只有你参与的那些，桥接页一次只看一个 Agent
而且是单边视角。两个 Agent 互相聊天恰恰是这个项目要证明的事 ——
看不见它，等于没有证据。

新增 `anthill/core/traffic.py` + 「Agent 往来」标签页。

**只读收件方的归档**（`agents/<名字>/mailbox/inbox/done/<日期>/`）。
一条消息在全网只会被投递到一个信箱，所以这个并集里每条恰好出现一次 ——
不重不漏是结构保证的，不靠去重。按 `thread` 分组，一段对话就还原了。

回执（`receipt.*`）不进正文：没正文，而且每说一句配一条，掺进去会把四句话
显示成八条、一半是空的。但也不悄悄扔 —— 每段末尾报「另有 N 条回执」。

页面上：时间戳到秒，日期只在变了的时候插一行（跨天的对话不标日期会读错先后）；
同一个人连着说的话不重复标名字；搜索和「只看 Agent 之间的」都在已取到的数据上
过滤，不重新请求。

### 顺带

- `panel_check.mjs` 加了一条结构检查：标签页、`PANES`、`<section>` 三者必须
  一一对应。漏掉其中一处的后果特别隐蔽 —— 那一页照常拉数据、照常渲染，
  **只是永远不显示**，而控制台干干净净。加这一页时就栽了一次。
- 桥接页的往来记录时间戳补上日期。
- 切工作区之后当前这一页立刻重新加载，不再等下一次轮询（那几秒里你看的是
  上一个工作区的数据，还以为是这个的）。

## [0.23.0] · 网页上看得见聊过什么；等消息不再占着会话

真机跑了一轮 tst1 ↔ tst2 之后暴露的三件事，外加一条作用域上的错。

### 桥接页多了「往来记录」

**干得越好，页面越空** —— 这是原来的行为。桥接页只显示 `inbox/`（还在等你回的），
而接上 Claude Code 之后会话几秒就回完了，消息立刻挪进 `done/`。
于是网页上没有任何证据说明这套东西正在工作。

记录一直都在，只是没人读它。新增 `anthill/adapters/bridge_history.py`
把归档读成一条时间线：谁找的、说了什么、回了什么、什么时候。

排序**按信封上的时刻，不按文件 mtime**。mtime 是归档时刻：一条没人回的消息会
一直躺在 `inbox/` 里，等对话结束才归档 —— 它的 mtime 比后来收到又秒回的消息
还新。真机数据里就撞上了（07:46 排到 07:43 后面）。这一栏读起来是段对话，
顺序错了就不是对话了。

### 粘给会话的那句话：放后台，别在前台等

前一版给的是前台 `--wait 300`。是真的在等了，但**整个会话被占住五分钟**，
期间连让它停下来都做不到 —— 为了收消息把会话废掉，代价太大。

现在给的是**后台**跑（`run_in_background`）：命令退出时 Claude Code 会自动
唤起会话，所以「有人找你」依然是推过来的，而等待期间会话照常归你用。
阻塞换来了推送，后台换回了会话。

### MCP 和 hook 都改成只对当前目录生效

原来给的是 `claude mcp add --scope user`（全局）和 `~/.claude/settings.json`。
两个都错了：你开的**每一个** Claude Code 都会挂上，而通常只有一两个要接进来。

改成 `cd <工作区> && claude mcp add anthill -- …`（`--scope local` 是默认值）
和项目里的 `.claude/settings.local.json`。

## [0.22.3] · 每行一条启动命令；一个会话挂几个工作区

用户两条建议，都采纳了。

### `ANTHILL_AGENT` 摆到网页上

「谁占着哪个」那张表，每行右边多一个**复制启动命令**（`ANTHILL_AGENT=cc2 claude`）。
用户说得对：**不想依赖自动认领的话，一律用它** —— 谁是谁一眼看得见，
也不用记名字。自动认领仍然是默认（什么都不配也能用），显式那条现在同样顺手。

### 一个会话同时挂几个工作区：本来就行

先验证再回答：两台 MCP server 各 `-w` 一个工作区，同时活着，
各自认领各自的桥接 Agent，互不干扰 ——

    server A: wsa / cc -> wsA
    server B: wsb / cc -> wsB

客户端按 server 名给工具分命名空间，两套 `anthill_*` 不会撞；
而每台 server 的自我介绍（`instructions`）里都写着自己是哪个节点、哪个工作区，
会话分得清谁是谁。这条不用改代码，只是以前没写出来 ——
现在面板和 QUICKSTART 都给了现成的两条命令。

（验证过程中顺带确认：`anthill init --node-name wsA` 会被拒绝，
大写字母不合法 —— 那是对的，只是我第一次写测试脚本时把错误吞了。）

## [0.22.2] · 钉死某一个时，不许悄悄把别人顶下线

用户确认「`ANTHILL_AGENT=cc2 claude` 就能指定接下来启动的 Claude Code 是哪个」——
是的。但顺着这句去验边界的时候，发现上一版留了个洞。

MCP server 那边写的是 `claim(layout, agent, force=True)`。于是
`ANTHILL_AGENT=cc2` 指到一个**还活着**的会话头上时，它会**悄悄把那个会话顶掉**——
两个会话同时是 `cc2`，读同一个收件箱、抢同一批消息，而各自的上下文完全不同。
那正好毁掉「一一对应」这件事本身，而且毁得没有任何提示。

改成默认绝不抢：被占着就直接拦下，并给出三条出路（换一个名字 / 面板上再建一个 /
确认那个会话不用了再 `ANTHILL_TAKEOVER=1` 接管）。**接管是显式动作**，不是默认。

实测：会话1 钉 cc2 拿到 cc2；会话2 也钉 cc2 被拦下；改钉 cc1 立刻拿到。

顺带把「同一个目录下开两个会话必须靠 `ANTHILL_AGENT` 区分」写进面板与 QUICKSTART ——
目录一样，自动认领的目录亲和性分不出谁是谁，这是那条机制的已知边界。

## [0.22.1] · 自动认领得「认回上次那个」，而且要报自己是谁

用户提的两个问题，第二个是真危险的：

> 自动认领会不会不好区分打开的 Claude Code？而且要是重启的话，
> 本来 a-a b-a，后面变成 a-b b-a，上下文混乱了。

### 重启之后别认错人

**上下文是挂在 Agent 上的** —— 它的邮箱、thread、以及别人对「cc1 说过什么」
的记忆。纯粹「挑一个空的」的话，A 是 cc1、B 是 cc2 的两个会话重启一轮、
顺序反了，就变成 A→cc2、B→cc1，历史直接串了。

需要一个**跨重启稳定**的身份。pid 每次都变，靠不住；**工作目录**是稳的 ——
`~/projA` 里重开的会话还是那个项目的会话。挑选顺序三档：

1. **上次就是我**（`claim.json` 里记的 cwd 等于当前 cwd）—— 认回去；
2. **谁都没用过的** —— 挑它，别去碰别人的历史；
3. 别人用过的 —— 实在没有了才接，这时候串上下文是不可避免的取舍。

第 2 档是自测时才发现要加的：只有第 1 档的话，先来的会话用完退出后，
后来的会顺手接管它 —— 于是「谁是谁」每开一次就洗一次牌。

`release()` 因此**不删记录，只把 pid 清零**：删了就等于每次重启重新洗牌。

### 报自己是谁

自动认领的代价是「你不知道这个会话拿到了哪个」。三处都补上：

- MCP：写进 server 的 `instructions`（会话一开口就知道自己是谁），
  同时往 stderr 打一行给人看；
- `anthill bridge` 自动挑的时候打印「这个会话 = cc1」；
- 面板桥接页那张表：谁占着哪个（pid + 工作目录），
  空闲的也标出**上次是哪个目录用的** —— 空闲不等于下一个会话随机拿到它。

实测：projA 与 projB 两个会话第一次拿到 cc1 / cc2；两个都重启、而且顺序反过来，
对应关系原样不变。

## [0.22.0] · 会话认领 + 阻塞等待：终端和桥接 Agent 一一对应

用户把三条缺陷点得很准，而它们的根其实是两个：

- **Claude Code 的配置粒度是「目录」，不是「会话」。** 把 hook / MCP 写进某个
  项目目录，那个目录下开几个会话就有几份一模一样的配置 —— 全都绑到同一个桥接
  Agent 上互相抢消息。「同一目录下两个会话各对应一个」这件事，配置文件表达不出来。
- **MCP 与 hook 都是拉取式的。** 模型自己决定什么时候调；会话闲着的时候，
  没有任何东西会把它叫醒。所以「粘一句话」那条路以前根本不是监控 ——
  是「你想起来了看一眼」。用户说「还不如之前的 monitor 方法」，说得对。

### 会话认领（`adapters/bridge_session.py`）

能穿透到单个会话的只有**环境变量**（子进程继承）。所以三级：

    显式指定 > $ANTHILL_AGENT > 自动认领一个还没人占的

最后那一级是「一一对应」的关键：面板上建三个桥接 Agent，开三个 Claude Code，
它们各自认领一个 —— **包括同一个目录下开三个**，因为认领跟着进程走，不跟着目录走。

认领落成 `bridge/claim.json`（pid + cwd + 时间）。判据是 **pid 还活着**：
会话关了、进程没了，那个 Agent 自动回到「没人认领」，下一个会话接手。
不用心跳、不用超时 —— 进程活没活着内核最清楚。

于是 MCP 配置变成**一次性的全局一条**（`--scope user`，命令里不写 Agent 名）。
以前给的是每个目录配一次、还写死名字，那正是「只能一个目录下一个终端」的来源。

### 阻塞等待

`anthill bridge <名字> --wait 300` 与 MCP 工具 `anthill_wait` 都会阻塞到
有人找为止。有了它：

- 粘给会话的那句话变成一个**真的监控循环**（「反复跑这条会阻塞的命令」）；
- MCP 那条路也能「一直盯着」了，不再只能靠人提醒它去查。

轮询而不是 inotify：这条路要跨 NFS（学校服务器的 home 就是），
而远端写入根本不产生本地 inotify 事件 —— watcher 那边为此专门做过降级。

### 面板

桥接页那块「把一个终端会话接进来」整个重写：一次性配置、`ANTHILL_AGENT` 钉死、
`anthill_wait` 怎么用，外加一张**谁占着哪个**的表（pid + 工作目录 + 空闲/已绑定）。

实测：同一个目录、同一份配置起两个 MCP server，一个拿到 cc1、另一个拿到 cc2；
`anthill_wait` 在消息第 2 秒到达时阻塞了 2.5 秒返回，没消息时等满超时。

## [0.21.7] · 「怎么把终端接进来」写在那一页上

用户：「那你在网页上写好，将终端加入 bridge 的方法。」

桥接那一页以前只给一句「盯着这个目录」的提示词 —— 那是三条路里**最被动**的一条，
而另外两条（hook 自动查收、MCP 原生工具）只写在文档里。人是在这一页上想起
「我要把 Claude Code 接进来」的，东西就该摆在这儿。

现在那一页顶上是一个可展开的「把一个终端会话接进来（三选一）」：

1. **粘一句给会话** —— 什么都不装，但是被动的；
2. **一个 hook** —— 让它自己查收。**真正去掉人肉转述的是这一步，不是 MCP**
   （MCP 工具也是拉取式的，装完之后会话依然不会主动知道有消息在等它）；
3. **MCP** —— 给会话原生的 `anthill_inbox` / `anthill_reply` / `anthill_send`。

三段都是可以直接粘的完整命令/配置，**路径由服务端填好**：`anthill` 在不在 PATH 上
（`uv run` 装出来的只在 venv 里）、工作区在哪 —— 页面猜不出来，而猜错的结果是
粘过去跑不通。每段一个复制按钮。

作用域也写在那儿：hook 写进那个项目的 `.claude/settings.local.json`、
MCP 用默认的 local 作用域 —— **别写进全局**，否则你开的每一个 Claude Code
都会来查这个收件箱。

顺带修了三处把 `**粗体**` 当字面量渲染进 HTML 的地方（那是 markdown，不是 HTML）。

## [0.21.6] · 桥接标签页在第二个工作区里不出现

用户：「没看到你说的桥接标签页。」单工作区是好的，我真开浏览器复现出了另一半 ——
**bridge Agent 配在第二个工作区里时，切过去之后标签页仍然不出现。**

`bridgeAgents()` 写的是 `state.nodes.find(n => n.local)` —— 一台机器照看好几个
工作区时，那句拿到的是「第一个」，不是你切过去的那个。于是它一直在问第一个节点
有没有 bridge Agent。待审批那一格是同一个写法，一起修的。

抽出一个 `focusedNodeData()`：先按焦点找，找不到才退回第一个。
**`find(n => n.local)` 这个写法本身就是坑** —— 多工作区支持进来之后它就不对了，
只是单工作区时看不出来。

顺带把两处配置作用域写进文档：`claude mcp add` 默认 local（只对这个项目目录生效），
hook 用 `.claude/settings.local.json` —— 都别写进全局，
不然你开的每一个 Claude Code 都会去查这个收件箱。

## [0.21.5] · 「切到这个」其实没切；侧栏改成工作区切换器

用户反馈：「按切到这个之后显然不对。」真开浏览器点了一遍，确实 —— `local.node`
变了，但**界面上一个字没动**。一次点击揪出三个 bug。

### 1. 焦点是客户端的事，界面却全看服务端

顶栏那个节点名读的是 `state.node`（服务端的**主节点**，省略 `?node=` 时的默认），
工作区行上的高亮读的是服务端算好的 `current` —— 两个都不看你在页面上选了谁。
于是切换只影响了 `scoped()` 拼出来的那几个接口，人完全看不出发生了什么。

现在顶栏和行高亮都跟着焦点走；不是主节点时行上会写明「主节点是 X」。

### 2. 本机的第二个工作区被显示成「连不上的对端」

总控视图合并各节点的视图时是**先到先得**。主节点排在最前，而它的 peers 里有一条
同名记录 —— 同一台机器上的另一个工作区被组播「发现」进来了。于是那一格被
「不可达的对端」占住，侧栏里点都点不了。

两头都修：合并时**本机那一格优先**；而且一个 serve 照看的别的节点根本不该被
「发现」成对端（它们在一个进程里，投递走本地文件），beacon 现在直接忽略。

### 3. WebSocket 每 2 秒把别的本机节点抹掉

`applyLocal` 里那句过滤写的是「留下所有**非本机**的」，也就是顺手把所有本机节点
都扔了，再把推送来的主节点加回去。一台机器只照看一个工作区时看不出来；
照看两个的时候，第二个每 2 秒闪一下。

### 侧栏改成切换器（用户提的）

原话：「左边默认是有一堆工作区的栏目的，可以点一下就下拉出对应工作区的详情。」

比顶栏那个**没有标签、还会隐藏**的下拉合理得多 —— 只有一个工作区的人从来没见过它，
「怎么切换」在界面上完全没有痕迹。现在本机每个工作区都是侧栏里的一组，
名字全摆着，点一下切过去并展开，别的收起来。

## [0.21.4] · 节点名跟着目录走；工作区怎么切换终于看得见

用户的原话：「现在有点奇怪，怎么切换工作区……而且节点默认名称可以是工作区名称。」

### 名字跟着目录走

一台机器上放着 `collab` 和 `collab-tst`，以前它们叫 `cs` 和 `cs-2` —— 什么也没说。
那是最早只考虑「一台机器一个工作区」时的选择，主机名当然够用。

现在默认取目录名。目录名说明不了事（`workspace`、`tmp`、`src` 这类）或者
非 ASCII 时退回主机名 —— 主机名「局域网里一眼对得上是哪台机器」那条理由仍然成立。
本机撞名照旧加序号（不同父目录下的同名目录会撞）。

`str.isalnum()` 对中文返回 True，所以 `我的项目` 会被当成合法名字放过去、
然后在 `NODE_NAME_RE` 那关炸掉 —— 这个坑在密钥的变量名上刚踩过一次，
这次一开始就按 ASCII 严格来。

### 跨机重名：闸设在配对那一步

按目录名起名之后，「两台机器上都有个 `collab`」变成了很可能发生的事，
而那会让 `to.node` 指谁彻底说不清（是我，还是它？）。

跨机唯一没法在起名时保证（两台机器互相不知道），所以闸设在配对：
`PeerRegistry` 现在知道自己叫什么，信任一个同名对端会直接拒绝并让人改名。
**这是原来就缺的一道** —— 实测确认过：改之前它照收不误。

### 怎么切换，终于看得见

切换器一直都有：顶栏一个下拉。但它**没有标签**，而且**少于 2 个节点时隐藏** ——
只有一个工作区的人从来没见过它，所以「怎么切换」在界面上完全没有痕迹。

- 顶栏那个加了「切换」二字；
- 工作区页每一行右边给出 **切到这个**（已接管的）或 **接管**（清单里有、
  但本进程启动之后才加进来的，点了就开始照看，不用重启 serve）——
  人是到那一页去找「怎么切换」的。

## [0.21.3] · 一键连目录一起删

用户要的：清单清干净还不够，目录也得能一次删掉。

批量删除最该有的一道闸不是「你确定吗」，而是**「你确定要删这几个吗」**。
所以这条路是两步：先 `GET .../workspaces/doomed` 算出名单、原样摆给人看，
删除请求再把**数目**带回来；对不上就整个拒绝 —— 名单在「你看」和「你点」之间
变了（另一个窗口刚加了一个工作区），这一次就作废，不会误伤。

三道不能破的：

- **只删 `.anthill/`**，不碰你放在那个目录里的别的东西（源码、笔记）——
  和单个删除同一条规矩；
- **本进程正照看的那些永远留下** —— 把自己删了，面板下一秒就找不着自己；
- **删不掉一个不该让剩下的都不删** —— 汇总上报，让人知道哪些还留着。

每个被删掉的目录都单独记一条 `panel.workspace_purged` 日志：
谁都可能第二天问「我那个工作区呢」。

页面上点之前会列出路径，真删文件那一下再确认一次。
浏览器测试里真的跑了一遍：该删的删了，`.anthill/` 之外的文件还在，
serve 自己那个工作区活着。

## [0.21.2] · 面板上一键清工作区清单，以及止住是我自己造的污染

### 先说我干的那件事

上一版让 `anthill init` 把工作区记进机器级清单（不登记的话面板看不见它、
重名检查也看不见它）。副作用没想到：**整个测试套件开始往开发者真正的
`~/.anthill/workspaces.json` 里写**，一轮跑下来攒二十几条
`/tmp/pytest-of-.../test_xxx0`。用户是被这个逼着来提需求的。

根因修法放在 `tests/conftest.py`：一条 autouse 的 `isolate_home`，
`Path.home()` 管本进程、`HOME` 管它 spawn 出去的子进程。
**autouse 是关键** —— 新写的测试不用记得这件事，才是真的防住。
这个坑踩过两次了（先是浏览器测试起子进程绕过 monkeypatch），这次一次性堵死。

假家目录建在 `tmp_path` **外面**：不少测试会列 `tmp_path` 的内容
（目录浏览器那几条），家目录混在里面会把断言弄脏 —— 加完立刻就红了两条。

### 一键清清单

面板「工作区」页多了两个按钮：

- **清掉失效的** —— 只清路径已经不存在的那些。最常见也最安全的一次清理，
  单独给一条路，省得为了扫垃圾把好的一起端了。
- **清空清单** —— 全清，但本进程正照看着的那些留下：把自己踢掉的话，
  面板下一秒就找不着自己了，那不是用户想要的「清理」。

**两个都只动清单，一个文件都不删。** 一次能带走好几个工作区的邮箱、黑板、
甚至密钥，而网页上的一次误点没有 undo。单个删除那条路仍然在
（`delete(..., purge=True)`），它一次只毁一个，而且要你先看清是哪一个。

## [0.21.1] · 修掉实跑时撞上的三个问题

用户在一台有约 40 个网卡的机器上起 `serve`，一次撞出三个。

### 对外广播的地址挑错了

启动打印 `http://172.19.0.1:45778`，而那台机器真正的局域网地址是 `10.15.3.61`。

`local_ip()` 的做法是「连一个远地址（10.255.255.255），看内核挑了哪条路由」——
那等于**问默认路由是什么**。而那台机器上 Docker 造了几十个 `br-*`，
默认路由走的是隧道 `sbtun0`，于是广播出去的是隧道地址：局域网里谁都连不上它。

改成先枚举网卡、排掉虚拟的那些（`docker*`/`br-*`/`veth*`/`tailscale*`/`zt*`/
含 `tun` 的 —— 真见过叫 `sbtun0` 的，`startswith("tun")` 漏得干干净净），
优先看着像真网卡的名字（`en*`/`eth*`/`wl*`），都排掉了才退回旧办法。

**但任何启发式都会有猜错的时候**，所以：`--advertise` > `[node] endpoint` >
自动识别，启动时打印是哪一种，自动识别时额外提示怎么覆盖。
猜错的表现是「别人连不上你」，那是个很难往回追的症状，不该让人靠猜发现。

### Ctrl-C 之后糊一段 traceback

`asyncio.exceptions.CancelledError` —— 看着像崩了，其实是正常退出。

uvicorn 默认在 `serve()` 里换掉 SIGINT/SIGTERM 的处理器，于是 Ctrl-C 同时触发
两条关闭路径（它自己的、和我们的 `stop` 事件），互相打断了 lifespan。
这个进程还管着信标、状态、拉取、定时四个循环，本来就该由我们统一收尾 ——
所以让 `capture_signals` 变成空操作，信号只由我们接。

### 一台机器上第二个工作区必然重名

`init` 两次都取主机名，于是第二个工作区起 serve 时被跳过，
只给一句「已经有一个叫 cs 的节点了」—— 用户既没做错什么，也不知道该怎么办。

节点名现在避开已占用的：`cs` → `cs-2` → `cs-3`。
`init` 那条路以前还绕过了去重（自己算名字），所以两条路收敛到同一个入口
（`suggest_node_name`）；`init` 也会把工作区记进机器级清单 ——
不登记的话面板看不见它，重名检查也看不见它。
真撞上重名时的报错也补齐了「怎么办」：改哪个文件的哪一行。

顺带：那句提示里的 `[node]` 又被 rich 当样式标记吃掉了（help 文本里踩过一次），
一并转义。

## [0.21.0] · 把欠着的做完

上一版结尾列的「还欠着的」，除了两条明确论证过不做的之外全做了。

### 编排能表达的形状

- **人工审批节点**（`PlanStep.needs_approval`）。审批那套 `security/approvals.py`
  本来就在，编排层却从没用过 ——「让人在关键一步之前确认」以前只能靠 Agent
  自己触发危险操作时被策略引擎拦下，非常间接。现在它是计划里的一等公民：
  没批过就先提交一条审批请求、不派活；批了往下走，拒了判这一步失败。
  **不阻塞等** —— coordinator 是事件驱动的，在 handler 里 await 一个要靠另一次
  tick 才会到的答复就是死锁；「等」的表达方式是这一步继续留在 pending。
  审批 id 从 task_id 派生且稳定，否则每次 tick 都提交一条新请求把目录刷爆。
- **条件分支**（`run_if`）：`ok` / `upstream_failed`（兜底、回滚、换条路）/
  `always`（收尾、清理）。**没有表达式语言** —— 那会变成一门要文档、要转义、
  要防注入的小语言，而计划是模型生成的，语言越花它写错的概率越高。
- **map 展开**（`for_each`）：在**计划校验阶段**展开成 `s__1` / `s__2`，
  正文里的 `{item}` 被替换。展开完仍然是静态 DAG，调度、看板、崩溃恢复、
  状态机一行都不用改；运行时展开的话那四样全都要处理「步骤数会变」。
- **每步超时**（`timeout`）：`attempts` 早就有了，超时却只有一个全局值。

顺带修了一个真的会卡死的：`tick` 以前只在超时/催办改动了状态时才 advance，
于是「卡在等审批」的运行永远没人再看一眼 —— 人批了也没用。

### 跑完之后

- **任务模板** `[templates.*]` + `anthill run --template review "src/x.py"`。
  跑得好的一次能存下来复用，不必每次重新用自然语言描述目标。
- **定时触发** `[schedules.*]`，由 `serve` 驱动。没做 cron 表达式 ——
  那是一门要解析、要测、要处理时区与夏令时的小语言，「每隔多久」够用。
- **完成通知** `[notify] webhook`。只做 webhook 一种：它能接到几乎所有别的地方去，
  而每加一种原生集成都是一份要维护的鉴权与格式。**默认全关** ——
  一个会自己往外发 HTTP 的框架得是用户明确要的，而且那条请求里带着任务目标与摘要。
  发不出去只记日志：通知失败不该让一次已经成功的协作看起来像失败了。
- **产物会被校验**：`finish` 声称的路径不存在就当场打回。以前它只是一串字符串，
  没有任何机制收集或校验 —— 模型报了个不存在的路径，派活的人拿到手才发现。

### 接进脚本

`--json` 补齐到 `status` / `agent list` / `peers list` / `dead list` /
`runs` / `cost` / `bridge`。`anthill runs --follow` 是 `anthill run` 按 Ctrl-C
之后的回程：协调器一直在磁盘上跑，这条命令重新盯上去（只读，关掉不影响协作）。

### 面板消息流

过滤、搜索、按级别筛、条数可调（80/300/1000）、导出 JSON。
导出的是**当前过滤之后**看到的那些 —— 导出一份和屏幕上不一样的东西最气人。

### 论证过不做的两条

- **子工作流**：把一步派给另一个 coordinator 本来就走得通（路由那层的事），
  不需要新概念。
- **循环迭代直到满足**：`done_when` 的返工轮次已经是那个语义。

## [0.20.0] · 两个 MCP 方向、hook、以及待审批终于看得见

排的四件事全做了。顺序有意义：**先做便宜的那个，再决定贵的值不值**。

### 1. `bridge --json` + hook（去掉人肉转述的其实是这个）

桥接的集成一直是被动的：面板给你一句提示词，你粘给会话，让它「盯着目录」。
会话不会被通知。

`anthill bridge <名字> --json` 加上一个 UserPromptSubmit hook
（`examples/claude-code-hook/`），一个文件就把「人肉转述」去掉了。

**这一条值得单独说清楚**：MCP 工具也是拉取式的，模型自己决定什么时候调 ——
装了 MCP server 之后 Claude Code 依然不会主动知道有消息在等它。
真正解决「被动」的是 hook，不是协议。两者互补，不是替代。

### 2. 面板上能看见待审批

`anthill approve` 一直没有面板对应物，连「有一条在等你批」这个信号都没有。
把安全边界画在「不能在面板上批」是对的，画在「看不见」就是缺陷 ——
agentd 停在那儿等，用户在任何界面里都不知道系统在等他。

任务看板顶上现在会显示待审批（**只读**，确认仍然只在 CLI）。
只看本机：别台机器的要走 `anthill approve --peer`，而且那些 prompt 里是命令原文，
没必要为了显示把它加进对外快照。

### 3. AntHill 作为 MCP client —— 给自己的 Agent 装外部工具

评审点了两条：工具集偏薄、`TOOL_FACTORIES` 是硬编码字典（加工具得改源码）。
自己造一套插件发现机制，是重新发明一个**已经有事实标准**的东西。

```toml
[mcp.files]
command = ["npx", "-y", "@modelcontextprotocol/server-filesystem", "/srv/data"]
risk = "medium"          # 默认 high

[agents.coder]
mcp = ["files"]          # 只给它声明过的那几台，不默认全给
```

三条安全约定：外部工具**默认 high 风险**（策略引擎照常管，无人值守时拒绝，
要用就显式降级、由人做判断）；名字加前缀（`files__read_file`，不撞名、
日志里一眼看出出了本进程）；**连不上只记日志，不让 agentd 起不来**。

连接是异步的而 handler 在同步的 `__init__` 里造，所以挂载放在 runtime 起循环前的
`setup()` 钩子里 —— 和 `tick` 一样用鸭子类型。

### 4. AntHill 作为 MCP server —— Claude Code 原生调它

`anthill mcp serve cc` 之后，会话有了 `anthill_inbox` / `anthill_reply` /
`anthill_send` / `anthill_runs` / `anthill_status`。走 stdio、由客户端拉起，
不开端口不加鉴权面 —— 能起这个进程的人本来就有这台机器的账号，
和 `anthill bridge` 同一个权限模型。

**只暴露桥接与只读查询**。改配置、启停 agentd、审批不从这条路出去 ——
那些的分量是「能在这台机器上执行命令」。
只有桥接 Agent 能被代表：别的 Agent 有自己的大脑，不该由这个会话代答。

### 真连一次才发现的事

MCP client 第一版用假对象测是绿的。真起一个外部 server 连上去，才发现
入参 schema 的字段名在 mcp 2.0 改了（`inputSchema` → `input_schema`），
`FastMCP` 也改叫 `MCPServer`。两个名字现在都认，测试也换成真起一个进程连 ——
**这类改名只有真跑才会现形**，和 M14 那次「面板在浏览器里根本没跑通过」同一个教训。

### 顺带

`anthill mcp tools` 列出外部 server 与谁在用；`mcp` 是可选 extra
（`uv sync --extra mcp`），不装不影响其余功能。

## [0.19.0] · 最后一米：面板上真能建出一个能干活的 coordinator

第二轮外部评审。核心判断是对的：底层能力（消息、传输、编排、面板）已经相当完整，
**卡的是最后一米** —— 数据算好了没接到界面上，能力做好了没接到入口上。
这一版基本都在接线。

### 最恶劣的那个：首次运行会「假装成功」

`init` 生成的默认模板里 `[agents.coordinator]` 只写了 `role`，没有 provider ——
按项目自己的规则，没有 provider 就是 echo agent。而 `_find_coordinator` 只按 role
找名字，不看它有没有大脑。于是 `anthill run` 把任务派给一个只会回显的 Agent，
拿回一句复读，然后打印「完成（ok）」、**退出码 0**。

拆解、派活、汇总一样没发生，用户却收到了成功信号。
**这比卡住 600 秒糟糕得多** —— 卡住至少能让人意识到不对劲。

- `run` 现在优先挑**有大脑的**那个 coordinator（模板自带的那个不会再被误选）；
  一个能用的都没有就直接拦下，并把该怎么配原样写出来。`--to` 指定的也一样检查。
- 模板里那个 coordinator 旁边加了注释，说清楚它现在还没有大脑。
- `anthill doctor` 会点名这件事。

### 面板建不出 coordinator（M10 承诺的最后一米）

「加一个 Agent」表单**没有 role 字段**，后端默认 `worker` —— 面板永远建不出
coordinator，而发起编排任务恰恰要求有一个。同时选 provider 大脑要求
`[providers.*]` 已在 node.toml 里配好，面板却**没有任何配 provider 的界面**。
于是想在面板上跑通旗舰功能，必须先去配置页手写 TOML。

- 表单补上角色选择。
- 新增「模型」页：配 provider（几家的预设一选就填好 base_url）、看每个 provider
  的密钥到底设没设、存密钥。
- 密钥落 `~/.anthill/secrets.env`（0600，家目录，和 peers.json 同一个待遇）。
  **`node.toml` 只存变量名这条规矩没动** —— 那个文件会进 git、会被 `fetch` 拉走、
  会显示在配置页上。密钥**只进不出**：没有任何读接口会回传它。
  不想落盘就照旧 `export`，面板显示「来自环境变量」；真环境变量优先级更高。

### 能接进脚本了

- `--json`：`runs` / `cost`（全项目此前没有任何机器可读输出）。
- **退出码有意义**：`send --wait` 超时或对方报错返回 2（以前和成功一样是 0）；
  `run` 没等到结果也返回 2。
- **`--plain` 真的边跑边打**。它以前是 `while await self._step(): pass` 跑完再
  一次性全打 —— `run --plain | tee log` 在任务结束前一个字都没有，默认超时 600 秒，
  恰好把这个 flag 唯一的用途（CI、tail -f、tmux 后台跑）全部破坏掉。

### CLI 补回面板已有的能力

M10 之后所有新能力都只进了面板，CLI 停在最初那批命令。对一个日常在 tmux 里
干活的人来说，停 agent、看历史任务、看花销只能靠 `ls`、`jq` 或者去开浏览器。

- `agent stop` —— 以前只有面板有，而 `start` 是脱离终端独活的，所以只能 kill。
- `agent ps` —— **这台机器上所有工作区**里在跑的 agentd。`status` 只看当前工作区，
  别的工作区里遗留的进程在任何界面里都不可见（实测遗留过五个跑了近两小时的）。
- `runs` —— 历史任务与每一步的产物、耗时、重试次数。`RunStore.all()` 一直存在，
  CLI 一处没用过；`run` 按 Ctrl-C 之后那次协作就从界面上消失了。
- `cost` —— token 用量与花费。用量一直在算、一直在写日志（`task.done ... tokens=N`），
  但没有聚合、没有折算、CLI 没有命令、面板上也看不到（`INTERESTING` 白名单里没有
  `tokens`，数据在最后一米被过滤掉）。单价来自 `[providers.*]` 的
  `price_in`/`price_out`，**没标价就只报 token，不瞎猜钱**。
- `doctor` —— 一次查完配置、密钥、邮箱、在跑的进程。`check_runtime()` 早就能查这些，
  但只在 `agent start` 时触发，得先让 agentd 失败一次才知道哪儿没配好。
- `guide` —— 按场景分组的命令地图。它以前是模块 docstring，`--help` 里看不到。

### 面板：算好了却没显示的那些

- 任务卡片补上**产物、耗时、重试次数、最终结果** —— 后端一直传着 `task_id`/
  `requester`/`result`，`state.py` 里还有 `artifacts`/`dispatched_at`/`finished_at`/
  `attempts`，渲染时全丢了。卡片只答得了「到哪一步」。
- 已完成任务从只留 3 条改成 20 条（第 4 条起以前在界面上永久消失）。
- 消息流把完整 ULID 缩成末 6 位，和 CLI 对齐 —— 以前一行的大半被无意义字符占满，
  右侧真正的错误信息反而被截断。

### 文档自相矛盾

`serve` 的 docstring 写着「找不到工作区会在当前目录建一个」，而同一个文件里
`_load_or_setup` 写着「没有就什么都不建」。实现是后者，前者是用户在 `--help` 里
看到的那句（M11 改动时漏改）。README 也跟着错。都改了。

### 顺带

- 强制 `watch_mode = "inotify"` 时，内核给 ENOSPC（别的进程把
  `fs.inotify.max_user_watches` 用光）会让 agentd 直接崩。现在降级轮询并说明原因 ——
  轮询慢一点，但节点还在收消息，崩掉就是彻底掉线。
- 错误输出加 `soft_wrap`，指纹列改成换行而不是截断 —— 指纹存在的唯一目的
  就是逐字符核对，被截成 `f1:c2:…` 就废了。
- **help 被 rich 吃掉的两处**：`--unattended` 那句「（不是「全部同意」）」被截成
  「…」（中文没有空格，rich 断不了行就直接截断，而这是个安全开关）；
  `peers invite` 的 `[node] name` 被当成样式标记删掉，显示成「里的  name」。
  前者加断点，后者转义成 `\[node]`。
- `--version` / `-V` 长选项（以前只有 `anthill version` 子命令）。
- **正文可以从 stdin 或文件来**：`anthill run -`、`anthill send coder @brief.md`。
  以前只能当位置参数传，稍长的 prompt 要么被 shell 的引号规则折磨，要么没法带换行。
  真以 `@` 开头的正文写成 `@@`。
- 开启 shell 补全。

### 没做的

- **MCP server 没做。** 评审说得对，那是和 Claude Code 结合的最大机会点，
  而且现有能力（邮箱读写、路由、状态查询）都齐了，只是换层协议外壳。
  但它是一个新子系统，不是接线，留到下一版。
- 工具注册表仍是硬编码字典（没有 entry points / 插件发现）。
- 编排仍只能表达「静态 DAG + 全局完成判据」：没有条件分支、循环、人工审批节点、
  map 展开、子工作流。评审指出人工审批节点和每步重试最容易加 —— 每步重试上一版
  做了，审批节点没做。
- 面板上仍看不到待审批信号；任务模板、通知、定时触发都没有。

## [0.18.0] · 一次外部评审之后的修复

一位评审者实测复现了 4 个确定性 bug，另有 2 个读码确认的架构级问题，
外加一批能力缺口与测试/文档缺口。值得单独记一笔的是它们的**分布**：
几乎全部集中在 M9–M14 这批后期新增的面板/编排代码里 ——
前面几层建立起来的安全与健壮性基线，后来的代码没跟上。

### 安全

- **面板 WebSocket 完全没有鉴权。** `panel_ws` 在 `accept()` 之前一行检查都没有，
  而它推的是和 `/api/state` 一模一样的 `build_snapshot()`：编排任务的正文、
  最近的日志、对端清单。REST 那个口子早堵上了，WS 这个同样的口子一直漏着。
  后果一：`--host 0.0.0.0` 下「写操作只对本机开放」的承诺在读方向被整个绕过。
  后果二：**WebSocket 不受同源策略约束**，浏览器里随便打开一个网页，那页面
  `new WebSocket("ws://127.0.0.1:45778/panel/ws")` 就能持续读走整个节点状态 ——
  默认的回环配置一样中招。`authorize` 改收 `HTTPConnection`（Request 与 WebSocket
  的共同基类），HTTP 抛异常、WS 用 1008 关掉。此前这个路由**一条鉴权测试都没有**。
- **一个伪造的 UDP 包能劫持已信任节点的全部出站消息。** `observe()` 是
  `endpoint or existing.endpoint` —— 来包带地址就无条件覆盖，不看是否已信任，
  而 beacon 收包没有任何认证，这个 endpoint 又正是投递用的 URL。
  「绝不影响信任状态」技术上没说错（`trusted` 确实保住了），但路由被换了，
  后果一样：局域网是明文 HTTP，任务内容直接泄露 + 静默 DoS。
  现在已信任的对端地址只认配对时那份，广播里自称的地址记进 `seen_endpoint`
  并在 `anthill peers` / 面板上标出来，由人决定要不要重新配对。

### 崩溃恢复的真实语义是「最多一次」

`_dispatch` 一进来就 `seen.mark()`，之后才跑 handler。kill -9 打在 handler
执行中间时，`recover_stale()` 确实把信从 `cur/` 退回了 `new/`，但重新处理时
`mark()` 返回 False —— 只补一条回执就 return，**handler 永远不会重跑**。
`mailbox.py` 的注释写着「seen.db 幂等正好兜住」，实际恰好相反：
seen.db 把重放抑制掉了，退信那一步是个安慰剂。
这一条直接否掉了文件邮箱架构最核心的卖点。

seen.db 改成两阶段：`claim()` 登记「开始处理」并记下是哪个**进程实例**领的，
`complete()` 在**信归档之后**才落。崩溃重启后令牌变了，claim 返回 RETRY，
handler 重跑；同一进程里并发撞上同一个 id 仍判 DUPLICATE。
令牌用 pid + 随机量（只用 pid 的话，机器重启后 pid 复用会把崩溃前的记录
误判成「本进程正在处理」，重放又被吃掉）。

### 另外三个确定性 bug

- `STATE_MARK` 漏了 `StepState.SKIPPED`，而渲染是下标取值 —— 并行 DAG
  一支失败、另一支还在跑的那一刻 KeyError，BOARD.md 从此停更、tick.failed 刷屏。
- `@app.post("/node/{name}/agents/{{agent}}/{{action}}")` 少了 `f` 前缀，
  注册的是字面量双花括号，**多节点远端启停从来没匹配上过任何请求**。
  更糟的是它的 404 被翻译成「对端没开远端管理」，反过来诱导人去打开
  `remote_admin` —— 那是整套里最重的开关。新增 `tests/unit/test_routes.py` 钉住整类。
- `fit_to_budget` 没清孤儿 tool 结果（而压缩那条路一直是清的）：切口落在
  assistant(tool_calls) 与它的结果之间时，两家 API 都直接 400。
  这是唯一一类「任务越接近成功越容易炸」的缺陷 —— 历史短时永远遇不到。

### 能力缺口

- **工具集从 6 个补到 9 个**：`edit_file`（精确原文替换，不唯一就拒绝）、
  `search_text` / `find_files`（受控只读检索，风险 LOW，无人值守下能用）、
  `read_file` 支持 `offset/limit` 翻页。
  此前 Agent 想在陌生代码库里定位一处实现只能一层层 `list_dir`（封顶 200 项），
  改一行要全量重写整个文件，文件后半部分永远读不到。
  `run_shell` 本可以顶上，但白名单只有七条验证类命令，白名单外一律 HIGH →
  无人值守时判 DENY —— 框架主打的场景下它形同虚设。
- **编排层真的读 `retryable` 了。** worker 明确回传的这个标志在整个 orchestrator
  里从没被读过，`StepRecord.attempts` 记了没人用 —— 一次网络抖动就是整个 run 失败。
  返工也不再无脑派给 `steps[-1]`（可能是个被 skipped、从没干过活的步骤）。
- **死信有了出路**：`anthill dead list / retry / drop`。此前 `dead_letters()`
  只被用来数个数，唯一的恢复手段是手动 `mv` 文件 —— 而进死信最常见的原因
  恰恰是「对端 agentd 晚起了十秒」这种修好就该重投的。
- **LAN 不再把「对端还没起来」判成永久失败**：`/deliver` 对「Agent 在配置里有、
  但邮箱还没建」改回 503（暂时性）而不是 404（永久）。完全相同的情形在 local
  传输里一直是可重试的，两条路不该给出相反的判断。
- **所有单调增长的目录装上刹车**（`core/retention.py`）：`done/` 按天目录整个删，
  `outbox/sent` 按 mtime 删，日志滚动成 `.1` 而不是截断。死信单独一个更长的
  保留期，删了要吼一声。启动清一次，之后每小时一次。归档量是消息量的两倍以上
  —— 每条业务消息还额外产生一条回执信封。
- **跨机回程不再靠人肉驱动**：`anthill pull` 的逻辑剥进 `transport/pull.py`，
  `serve` 每 60 秒自己拉一次（`[runtime] auto_pull_seconds`）。

### 测试与 CI

- **CI 从来没跑过浏览器测试。** 没有 `playwright install`，那 9 条投入最大、
  抓 bug 最多的测试会 importorskip 通过、然后在 `launch()` 静默 skip ——
  M14 那两个「藏了很久」的 bug 如果重现，CI 照样全绿。装上 chromium，
  并加 `ANTHILL_REQUIRE_BROWSER=1`：装好了还跳过就判失败。
- **CI 加 `timeout-minutes: 20`** —— 集成测试里有近百处轮询等待。
- 两个 provider 的 `complete()`（也就是真正发出去的请求体）补测：
  61% → 92%，62% → 94%。**真模型带子仍然没有** —— 那要 API key、要联网、要花钱。
- 修掉 `test_replying_archives_both_the_request_and_the_draft` 的偶发失败：
  等的是「结果到了」，断言的却是「归档完了」，而回信是先发后归档。

### 文档

roadmap 补上 M11–M15（此前只存在于 CHANGELOG）；README 从「M0–M10、657 个测试」
更到「M0–M15、985 个测试」；PRD 非目标里「Web 面板只读」这条已被 M7/M10/M13/M14
推翻，明确标注而不是删掉 —— 范围是需求推着变的，不是偷偷扩的。

## [0.17.0] · 桥接搬到面板上：加它的地方和用它的地方是同一个地方

用户的原话：「我在网页配置加入 Claude Code，结果还要手动去和终端说；
而且在网页加了 bridge 并启动之后那个目录也没那个文件。」

后半句是场误会 —— 目录（`bridge/inbox`、`outbox`、`done`、`pending`）在启动时
就建好了，只是**没有消息就是空的**。但前半句戳中了真问题：桥接 Agent 是给人留的口子，
而在网页上加完之后，页面上一点痕迹都没有，还得切到终端交代一遍「盯着那个目录」。
**加它的地方和用它的地方不是同一个地方。**

### 新增

- **面板多了「桥接」标签页**（没有桥接 Agent 时不显示，免得点开一片空）：
  - 在等这个人回的消息列在那儿，带发件人、类型和正文摘要；
  - **回复直接在页面上写** —— 不用开终端，也不用有 Claude Code；
  - 也能**主动发一条**给任何人（桥接设计的副产品：outbox 里放一个带 `to:` 的文件）；
  - 顶上是那句该粘给常驻会话的话，路径已经拼好，带「复制」按钮。
- `GET /panel/api/bridge/{agent}`、`POST .../reply/{msg_id}`、`POST .../speak`。

页面写下去的还是 `bridge/outbox/` 里那个文件，和手写的完全一样 ——
所以**两条路完全并存**：会话照样盯着目录，你也可以在网页上先替它回一句。

### 测试

- 接口层五条（含「非桥接 Agent 没有这个收件箱」和「不能回一条没人问的消息」）；
- 浏览器里真点一遍：队列渲染、页面上回完落成文件、没有桥接 Agent 时标签页隐藏。

## [0.16.1] · 修掉「全新机器上配不了工作区」的死结

用户起了一下就撞上：面板上没有配置节点和工作区的入口。

### 修复

- **全新机器（还没有工作区）上，工作区与配置标签页根本不出现。**
  页面用「能不能读到 `api/config`」来判断写入口挂着没有，可那个路由有两种失败：
  **没有写权限**是 404，**还没配工作区**是 409 —— 混在一起，于是一台新机器被判成
  「不能写」，而那个**专门给「还没有工作区」准备的**设置界面反而永远出不来，
  没有任何办法把工作区配起来。现在有一个专门的能力探测点
  `GET /panel/api/can-write`，它不看节点状态，只回答「你能不能写」。
- **浏览器测试往开发者的家目录里写东西。** 工作区清单和面板令牌都落在
  `~/.anthill/`，而这些测试是起子进程跑的，`Path.home` 的 monkeypatch 拦不住。
  后果不只是脏：上一轮测试建的工作区会被下一轮的「全新机器」认回来，
  于是它根本不新 —— 这正是这个 bug 第一次没被测出来的原因。
  现在给子进程换 `HOME`。

### 测试

- 浏览器测试补上**未配置**那条路径（此前每个用例都先建好了工作区，
  所以恰好绕开了出问题的那一条）：全新机器能不能看到设置界面、
  能不能在页面上把工作区建起来。

## [0.16] · M14 面板重做：温纸风格，以及它其实一直没在浏览器里跑通

用户说面板「很简陋而且比较丑」，要按 Claude / ChatGPT 网页版那种风格重做。
重做完用真浏览器打开一看 —— **面板此前在浏览器里根本没跑通过**，而所有测试都是绿的。

### 修复（两个藏了很久的 bug，只有真打开页面才会现形）

- **相对路径解析错了。** 页面挂在 `/panel`（没有尾斜杠），所以 `fetch("api/cluster")`
  解析成 `/api/cluster` 而不是 `/panel/api/cluster` —— 404。
  也就是说**拓扑、任务、消息流一直是空的**，右上角一直显示「连接不上」。
  写接口带了 `panel/` 前缀所以没事，读接口全废了。现在统一用一个函数按页面路径拼。
- **WebSocket 从来没通过。** `uvicorn` 不带 ws 实现时，升级请求被当成 404 ——
  而 `websockets` 从来不在依赖里。实时推送形同虚设，一直在靠 5 秒轮询兜底。
  补进依赖。
- **`[hidden]` 被 id 选择器盖住了。** `#pane-chat{display:flex}`（1,0,0）压过
  `.pane[hidden]{display:none}`（0,1,1），于是对话输入框在别的标签页上也露着。
- **每 2~5 秒整块重写 innerHTML**，鼠标悬停状态没了、正要点的按钮从指针底下被换掉，
  点击直接落空。内容没变就不动 DOM。

curl 测的是接口，ASGI 传输测的是路由，**都测不到「浏览器怎么解析相对路径」**。

### 新增

- **`tests/integration/test_panel_browser.py`**：用 playwright 真开 chromium
  点一遍 —— 数据能不能加载、WS 通不通、加/启/停 Agent、发消息、每个标签页、
  侧栏有没有横向溢出。没装 playwright 就跳过，不挡别人跑测试。

### 界面

方向定为**温纸 + 技术排版**：Claude 那种「像一份排得好的文档」的松弛感，
但保留排查工具该有的信息密度 —— 松弛给容器与留白，密度给日志、步骤、Agent 卡片。

- 布局从「四块挤在一屏」改成**侧栏（拓扑）+ 主区（五个标签页）**：
  任务看板 / 对话 / 消息流 / 工作区 / 配置。配置和工作区不再挤在底部。
- 暖中性纸色，陶土色只表示「主行动 / 你在哪儿」；绿琥珀红严格只表状态。
  深色模式是暖炭色，不是蓝黑。
- 字体三档配对：衬线（标识与栏目名）/ 无衬线（界面）/ 等宽（**只**给机器数据）。
  面板必须能在没有外网的服务器上打开，所以只能用系统字体 —— 但仍然有配对策略。
- 层叠与极轻阴影代替到处描边；hover / focus / active 都有设计过的反馈；
  切标签页有 200ms 的入场，且尊重 `prefers-reduced-motion`。

### 已知限制

对话标签页只列**从面板发起**的会话 —— `anthill chat` 或 `anthill send` 起的
对话不在里面（会话记录只在面板发消息时写）。消息流里看得到，对话列表里看不到。

## [0.15] · M13 面板令牌：没有显示器的机器也能被操作

用户一句话点破了一个设计缺陷：**「61 机器没有接显示器，根本操作不了」**。

面板的写操作一直只认「连接来自回环」。那条判据的真实含义是「你是这台机器的
主人」—— 在笔记本上成立，在机柜里的服务器上直接崩掉：**你没法在它上面开浏览器**。
更糟的是，一台**全新**的无头机器还没配过对，连节点之间那条签名通道也用不上，
等于完全够不着。当时 17 个面板接口只认本机，只有 3 个能从别的机器操作。

### 新增

- **`anthill serve --panel-token`**：本机生成一个访问令牌，启动时连着 URL
  打印一次。带着它就能从任何一台机器操作这台 —— 建工作区、加 Agent、启停
  agentd、改配置、发起任务、对话，全都能做。回环仍然直接放行，本机不受影响。
- 令牌可以走 `Authorization: Bearer`、`X-AntHill-Panel` 头或 cookie；
  URL 上的 `?token=` 只用于第一次落地，页面拿到后立刻存起来并从地址栏抹掉
  （URL 会进浏览器历史和 referer）。

### 安全

- **令牌绝不从命令行参数传。** `ps` 是所有人都看得见的。它由本机生成、落在
  `~/.anthill/panel-token`（0600），要覆盖用环境变量 `ANTHILL_PANEL_TOKEN`。
- **说清楚它的分量**：拿到它就能改 node.toml、就能加一个带 `run_shell` 的
  Agent —— 等价于「能在这台机器上执行命令」，和一把 SSH 私钥同档，
  不是"网页密码"那一档。
- **明文 HTTP 上它会被嗅探到**，这一点写在启动提示里：局域网不可信时应该用
  `ssh -L` 转发端口，而不是开着令牌裸奔。
- 同源检查不因为有令牌就放松 —— 令牌可能被别的页面偷去用。
- **`/panel/api/state` 补上了认证。** 它一直是敞着的（当初想给网段上的人留个
  只读视图），可它给出的是编排任务的正文和最近的日志 —— 和 `/node/summary`
  给对端看的是同一批东西，那边要签名，这边不该裸奔。有了令牌就没必要再留这个口子。

## [0.14] · M12 多路复用：一台机器一个 serve、一个端口、管全部工作区

用户直接质疑了上一版的做法：**「每个工作区都要起 serve 吗？不能一个机器上的来管理
全部工作区吗？工作区之间是隔离的」**。他是对的，而且理由恰恰在隔离本身：

**路由键 `to.node` 本来就写在信封上**，工作区之间又不共享任何状态 ——
所以一个进程按名字把信分派到不同工作区的邮箱，就是一张查找表的事，
不需要一个工作区一个进程、一个端口。

### 新增

- **一个 serve 照看多个节点**（`web/context.py` 的 `NodeRegistry`）：
  命令行指的那个是主节点，清单里登记过的其它工作区一并接管。
  `/deliver` 按 `to.node` 分派 —— 用**那个节点自己的** peers 验签、
  往**它自己的**邮箱里放。密钥是跟着工作区走的，这一点在多节点下尤其要守住。
- **按节点寻址的端点**：`/node/{name}/summary`、`/node/{name}/config`、
  `/node/{name}/agents/{agent}/{action}`。签名签的是完整路径，
  所以「问 boxb」的签名换不到「问 boxa」。不带名字的旧路径保留，指主节点。
- **面板顶栏多一个本机节点选择器**（只有真的多于一个时才出现），
  状态、对话、配置、加 Agent 都跟着切。
- 广播、状态文件也按节点各来一份 —— 同网段的人该看见的是「节点」，不是「进程」。

### 修复

三个都是联调时真撞出来的，一个比一个隐蔽：

- **`atomic_write` 的临时文件名是固定的 `.part`，两个写者会互相偷。**
  信封文件名是 ULID，天生不撞；但 `peers.json` / `status.json` 是固定名字，
  A 写好 `x.part`、B 也写 `x.part` 并先 rename 走，A 再 rename 就找不到自己的
  临时文件 —— 报一个莫名其妙的「No such file or directory」。
  一个节点上跑着 serve 和好几个 agentd，这种并发是常态，不是意外。
  临时名改成带 pid + 序号。
- **`--host 0.0.0.0` 时把 `http://0.0.0.0:45778` 当地址广播了出去。**
  那是绑定用的通配符，不是别人能连的地址 —— 对端老老实实记下来，然后永远连不上。
  现在绑通配符时自动取一个对外可达的本机 IP。
- **对端之间的 HTTP 走了系统代理。** 代理是用来出网的，而目标是自己局域网里的
  另一台机器。更糟的是 `no_proxy` 里的 `10.0.0.0/8` 这种网段 httpx 不认
  （它按主机名匹配），于是 `10.15.3.61` 被判成要走代理，换回来一个空的
  `HTTP 502：`，根因完全看不出来。现在 peer 之间的客户端一律 `trust_env=False`。

## [0.13] · M11 远端启停、不再擅自建工作区、多工作区清单

用户三句话，一句比一句往根上问：**远端的 agentd 也要能在面板上启停**、
**启动面板时不一定非要初始化工作区**、**能不能一个面板管多个工作区**。

### 新增

- **在面板上启停别台机器的 agentd**：走和「改它的配置」同一道闸（对方的
  `remote_admin`）—— 能改 node.toml 本来就约等于能在那台机器上执行命令，
  不必再分一级。签名签的是**完整路径**（含 Agent 名和动作），
  一次「启动 coder」的签名换不来「启动别人」或者别的动作。
- **`serve` 撞上空目录不再擅自建工作区**，改为进「未配置」状态：
  面板给一个目录浏览器，你挑好地方再建。**在那之前磁盘一个字都不写。**
  上一版会就地建 —— 装好就能用是对的，但「就地」这个决定不该由程序替人做：
  你可能只是 cd 到了别处，结果 `.anthill` 撒在一个根本不想要的目录里。
  显式给了 `-w` 仍然是「没有就建」，那是明确的意思表示。
- **认下工作区之后，同一个 serve 进程就地活过来**，不用重启（`NodeContext`）。
  刻意**只支持「没有 → 有」这一次转变**：peers 与密钥都跟着工作区走，
  中途换等于换身份，已经跑着的 agentd 和已经建立的信任关系全都对不上。
- **工作区清单**（`~/.anthill/workspaces.json`，不属于任何一个工作区）：
  面板上能看到这台机器上有哪些工作区、建新的、移出清单、或者连文件一起删。
  删除刻意两档 —— 默认只是不再显示；`purge` 才真删，而且**只删 `.anthill/`**，
  不碰你放在那个目录里的代码。

### 修复

- **非法节点名会留下一个「看着像工作区、其实起不来」的目录** ——
  先写 node.toml 再校验，写完才发现名字不合法。改成先验名字再动盘。
- **端口被占时 `serve` 退出码是 0**（上一版遗留）：现在开跑前先探端口。

### 重构

- `web/app.py` 拆成 `app.py`（节点对节点的端点）+ `panel_routes.py`（面板）。
  678 行逼近 800 上限，而这两拨东西的读者、威胁模型、变更节奏都不一样。

### 下一步（用户已提出）

一个工作区一个 serve 进程是笨办法 —— 路由键 `to.node` 本来就在信封里，
工作区之间又是隔离的，所以**一个 serve 完全可以按节点名把信分派到不同工作区**。
本版先把清单和「未配置」这条路铺好；多路复用另起一版做。

## [0.12] · M10 面板即控制面：装好就能用，单机不必开终端

用户的原话：**「启动之后可以通过面板来创建工作区什么的，单机的话也能直接用 web 面板操控」**。
起因是他在一个空目录里跑 `anthill serve`，撞上「先跑一次 anthill init」——
那个错误把「先在终端做点什么」当成了前提，而这正是要去掉的东西。

### 新增

- **`anthill serve` 找不到工作区就自己建一个**，不再报错退出。建在哪、
  节点叫什么名字都打印出来。新机器上装好就能直接开面板。
  `anthill init` 留着 —— 「我想指定建在哪、叫什么」仍然是合理需求，
  只是不再是必经的第一步。建工作区的逻辑挪进 `core/workspace.py`，两边共用一份。
- **在面板上加 / 删 Agent**：拓扑那一栏底下多了个表单，选大脑类型
  （echo / bridge / command / provider）就行。改的是 node.toml，
  所以走和手改配置一样的校验与备份。
- **在面板上启 / 停 agentd**：Agent 卡片上多了按钮。
  这是单机场景下最后一处非用终端不可的事 —— 以前每个 Agent 要一个终端窗口。
  用 `sys.executable -m anthill` 拉起（新增 `anthill/__main__.py`），
  不依赖 `anthill` 在不在 PATH 上；`start_new_session=True` 让它脱离面板独活。

### 修复

- **面板加完 Agent，它既不出现在面板上、也收不了跨机消息。** serve 手里捧着
  启动那一刻读到的 config，而 node.toml 现在是运行期可改的（面板加 Agent、
  远端管理、人手动编辑）。`/deliver` 拿旧 config 判收件人，直接回 404
  「本节点没有这个 Agent」—— 对面看着面板上明明有，完全不知道差在哪。
  新增 `ConfigRef`：按 mtime 重载，和 `peers.json` 一个套路
  （**文件是唯一真相，内存里那份只是缓存**）；改坏了就继续用上一份好的，
  别让一次手滑弄停整个节点。
- **加了 Agent 却没给它建邮箱**，于是它在自己的 agentd 第一次启动之前收不了信。
  「配置里有它，就该能收它的信」—— 任何一次配置写入之后都补一遍，幂等、顺带自愈。
- **面板把 bridge / command Agent 显示成 `echo`**，让人以为它不干活 ——
  背后其实是一个人。这个 bug 在 CLI 的 `agent list` 上修过一次，面板又犯一遍，
  因为判定逻辑有两份。现在共用 `core/config.brain_of`。
- **端口被占时 `serve` 退出码是 0**，脚本和 systemd 都会以为起成功了。
  现在开跑前先探一下端口，占了就给一句人话（还告诉你换哪个端口），退出码 1。
  其他后台任务异常退出同样让退出码反映出来。

### 安全

- 加 Agent 的表单会被写进 node.toml 再被解析 —— `persona` 里的引号做转义，
  否则那就是一次配置注入（有测试钉着：塞一段 `[agents.evil]` 进去，解析后必须没有它）。
- 启停进程比改配置还直接，所以和其他写操作同一条闸：显式 `--panel-write`
  + 逐请求校验来源是回环。网段上的人拿到 403。
- 面板拉起的 agentd 带 `--unattended`：它前面没有人，需要确认的高危操作一律拒绝。

## [0.11.1] · 修掉一个把 M9 卡死的开关冲突

### 修复

- **`--panel-write` 和 `--host 0.0.0.0` 不再互斥。** 跨机投递必须绑对外，
  而写权限以前只允许配回环 —— 结果总控那台**没法既收别的机器的消息、
  又在面板上操作**：选前者面板是只读的（M9 那些功能全点不了），
  选后者对方的回信进不来（在面板上说一句，回复永远不出现）。
  等于 M9 做的东西在双机场景里根本用不上。
  启动期那道限制挡不住任何真实攻击 —— 守门的一直是**逐请求**的来源校验；
  它只挡住了正常用法。现在去掉，并在启动时明确打印「写操作只对本机开放」。
- `--panel-write` 现在隐含把面板打开，不必再多写一个 `--panel`。

### 安全

- **显式关掉 uvicorn 的 `proxy_headers`。** 面板写权限是按「连接来自本机」判的，
  就不该让 `X-Forwarded-For` 有机会改写那个值。默认值恰好是安全的
  （只信任来自 127.0.0.1 的转发头），但那是巧合，不该靠。
- **加一道同源检查**挡跨站发起的请求。说清楚它挡的**不是**一个现成的洞：
  写接口只收 JSON body，浏览器发跨源 JSON 请求前必须先预检，
  而本服务没有任何 CORS 响应头，预检就过不去。加这道只是不把安全性
  寄托在那个间接保证上 —— 浏览器策略会变，而「同源才放行」是自己能守住的。
  没带 Origin 的请求（curl、脚本）照常放行，它们还得先过回环那一关。

实测（绑 `0.0.0.0` 的真服务）：本机 200 / 网段地址 403 /
本机但 `Origin: http://evil.example` 403 / 网段地址伪造 `X-Forwarded-For: 127.0.0.1` 403。

## [0.11] · M9 自动一点：默认可见、PIN 配对、面板上对话与远端配置

用户提了三条，都是「机器一多就嫌麻烦」：
**serve 之后就该能互相看见**、**面板要能配别的机器和发起任务**、
**能在面板上跟任意一个 agent 直接对话**。

### 新增

- **默认可见**：`discovery.enabled` 从 false 翻成 true。同网段的 anthill 节点
  互相能看见（广播包里只有节点名、Agent 名单、地址）。
  **可见 ≠ 可通信这条线没有动** —— 要互投消息仍然必须有人在两边各看一眼。
  彻底隐身的路一直留着（`enabled = false` 时不发包、不监听、连 socket 都不创建）。
- **六位 PIN 码配对**（`anthill peers pair`）：不用再复制一百多字符的令牌。
  A 机开窗口显示 PIN，B 机输进去，一次往返换完，两边各自核对指纹。
- **在面板上跟任意机器的任意 Agent 对话**：消息流那一栏多了「对话」标签页，
  选一个 Agent 就是一个会话，接着同一个 thread 往下说（对方的记忆才连得上）。
- **面板可以配别的机器**（`--remote-admin` / `[security] remote_admin = true`）：
  配置抽屉多了一个「哪台机器」的下拉框，读写都走签名请求。
- **面板上就能配对**：拓扑里「见过但还没配对」的节点带一个配对按钮，
  点了要么显示本机 PIN，要么让你输对方的 PIN。

### 安全

- **PIN 配对用 PAKE（spake2），密钥从不上网线。** 这是整套东西成立的前提：
  把 PIN 加密的密钥直接发过去等于没加密 —— 抓包的人离线暴破六位数字是秒级的事。
  SPAKE2 让双方**各自推导**密钥，线路上既没有密钥、也没有可离线暴破的密文。
  在线猜测则由「一个窗口只有一次机会」堵死。
- **配对带密钥确认。** SPAKE2 在口令不符时不报错，只是两边各得一把不同的钥匙 ——
  少了这一步，PIN 打错会配成「看起来成功了、之后每条消息都验签失败」，
  是最难查的那种状态。现在 PIN 不对时两边都不落库。
- **配对窗口是文件（0600）且两分钟过期**：开窗口的是 CLI/面板，接请求的是 serve，
  跨进程只能走文件；窗口里存着 PIN，那两分钟里它等价于密钥。
- **远端管理默认根本不存在（404）**，不是「存在但会拒绝」——
  和面板写入口同一条原则：别给人留一个可以试探的门把手。
  开启后每一次读写都写审计日志（谁、什么时候、备份在哪）：
  配置被改坏时，那条日志是唯一能回答「谁干的」的东西。
- **对话内容和总控视图一样只对本机开放**：面板绑 `0.0.0.0` 是跨机投递的需要，
  但对话内容不该跟着摊给整个网段。
- thread id 会被拼成文件名，只认 ULID —— 别让它变成一次路径遍历。

### 修复

- 一条偶发失败的测试：同一个信封连投三次，而同一个 id 就是同一个文件名 ——
  上一次还没被取走就重投等于把文件覆盖掉，watcher 只会看到一次。
  改成一次一次地等。这是测试自己造的竞态，不是实现的问题。

### 说清楚代价

打开 `remote_admin` 之后，「信任一个对端」的含义就从「它能给我投消息、
我的 Agent 会审」变成「**它能接管这台机器**」—— 能改 node.toml 就能加一个带
`run_shell` 的 Agent。所以它默认关着，而且开的时候终端会明确告诉你这一点。
想要逐次点头的话，M5 那套 `approvals/` 审批流仍然在，两者是并列的两条路。

## [0.10] · M8 一个面板管所有机器

### 新增

- **总控面板**（`web/cluster.py`）：随便挑一台开面板，它把所有**已信任**节点汇到
  同一页。拓扑按机器分组，任务看板与消息流各多出一列「在哪台机器上」。
  之前每台机器只能各看各的，机器一多就没人看了。
- **汇总只取一个文件**：每个节点由 `anthill serve` 定期把自己的快照写成
  `.anthill/status.json`（原子写，权限 0600），总控按对端本来的接法去取 ——
  局域网 peer 走 `GET /node/summary`，SSH peer 直接 SFTP 读那个文件。
  逐个文件走 SFTP 拉太碎，共享文件系统又不存在，所以约定这一个汇合点。
- **`GET /node/summary`**：只读，给已信任的对端看。认证和投递同一把共享密钥 ——
  签 `域标签 + 节点 + 路径 + 时间戳`（`security/signing.py` 的 `sign_request`）。
  `peers trust` 配好之后不需要任何额外配置。不想共享就 `anthill serve --no-summary`。
- **`anthill serve --no-summary`**：关掉状态共享（端点直接 404）。

### 安全

- **对端传来的快照是外部输入，一律先过 `web/status.py` 的 pydantic 模型**。
  那些字段最终会被拼进面板页面的 HTML —— 一个声明成整数的 `queue` 字段
  如果放过一个字符串，就是一次 XSS；而面板开了写权限时，
  「在面板里执行 JS」约等于「在这台机器上执行命令」。
  长度与条数也都封顶，免得对面用一个几万条事件的文件把页面撑死。
  前端每个插值点同时转义，两道都要有。
- **未信任的节点只出现在拓扑里，总控不会去读它的状态** —— 发现 ≠ 可通信，
  这条线在读取方向上同样成立。
- **`/panel/api/cluster` 逐请求校验来源是回环**。它是个有副作用的 GET
  （每次可能触发 N 次对外连接），而且把**所有**对端的状态汇到一处 ——
  面板绑 `0.0.0.0` 时那等于摊给整个网段。页面拿到 403 会自动退回只看本机。
- **请求签名的时间窗收到 30 秒**（信封仍是 5 分钟）。信封要宽，是因为邮箱是
  存储转发队列；读请求是同步的，而且没有 seen.db 那样的去重兜底，
  窗口有多长，在明文 LAN 上抓到一个包的人就能重放多久。
- **签名材料带上域分隔标签**，请求签名与信封签名共用同一把密钥，不能互相顶替。
- **认证代码不再被畸形输入打成 500**：HTTP 头按 latin-1 解出来，
  一个 0xFF 字节就能让 `compare_digest` 抛 TypeError；不带时区的时间戳
  会让日期相减抛 TypeError。两处都接住 —— 这段代码在认证**之前**
  就要面对陌生人给的字节，换来一段 traceback 的话，连密钥都不用有的人就能触发。
- **共享出去的快照不含 peers 列表**：对端做总控面板不需要知道我还认识谁，
  而那份列表里带着别人的指纹与地址。同时把端点文档改成如实描述给出去的是什么
  （含日志的 `error` 字段）—— 把一个节点标成 trusted，
  意味着它既能给你投消息，也能看你在干什么。
- 写权限只对总控这台机器生效：它发起的任务从本机 `cli` 出发，跨机那一段仍然走
  签名投递；总控读得到别人的状态，但改不了别人的配置。

### 稳定性

- **一台机器连不上不会把整页卡住**：并发拉取、各自超时；已经有旧数据的节点
  直接返回旧数据、后台去刷。实测拔掉一台之后总控页面 0.18 秒返回。
  分布式面板最常见的坏法就是一台机器挂了、整个页面转圈。
- **按节点缓存**（`ClusterCache`，10 秒）。浏览器每 5 秒轮询、多开一个标签页翻一倍，
  而远端本来也只每 5 秒写一次自己的快照 —— 每次轮询都去连一遍每台机器
  （SSH 还得重开连接）足够在学校服务器上撞上 `MaxStartups` 或 fail2ban。
- **停更的快照不再显示成绿灯**。SSH 那条路是直接读对方磁盘上的文件，
  那台机器上的 serve 被 kill 之后文件还在、读得到 —— 不看时间的话，
  一台死了一周的机器会一直显示成「已连上、Agent 全在跑」。
- **SSH 的取数超时给到 25 秒**（HTTP 仍是 6 秒）。SSH 握手本来就比一次 HTTP 慢，
  按 HTTP 的窗判，等于把所有握手慢一点的机器永久判成「连不上」。
- **对端返回的内容有大小上限**（512 KiB，边收边数）：不封顶的话，
  一个被改过的 `status.json` 就能把总控进程撑爆。
- **写状态失败不会把 `serve` 带走**。此前只接 `OSError`，而 `atomic_write` 抛的是
  `MailboxError` —— 磁盘一满，写状态的任务就崩，`serve` 跟着退出，退出码还是 0。
  现在接住所有异常；同时补上 `serve.task_crashed` 日志，
  后台任务是**炸掉**才停的要说出来，别再表现成「正常退出」。

### 修复

- **WebSocket 推的是本机快照，会把总控视图里其他机器抹掉** —— 页面打开时能看到
  三台机器，一秒后只剩本机。改成两路数据合成一张图：WS 只更新本机那一格
  （保留 kill -9 之后立刻变灰的手感），全集群走 5 秒一次的 `api/cluster`。
- **跨零点时消息流会卡住**：事件的 `ts` 只留了 `HH:MM:SS` 给人看，
  合并多台机器的时间线时按它排序，零点后的新事件会被排到最前面、然后第一个被截掉。
  另存一个完整时间戳专供排序。
- **`anthill status` 把 `peers trust` 建立的对端报成「未配置」** —— 它只看
  node.toml 里的 `[peers.*]`，看不到 `peers.json`。这条命令是「为什么收不到消息」
  的第一站，报错比不报还糟。现在两个来源合起来显示，并标出「已信任 / 仅发现 / 配置」。
- 一条偶发失败的适配器测试：它等的是「有回信」，但回执是在跑子进程**之前**
  就发出去的，于是断言时子进程还没把文件写出来。改成等 `task.result`。

### 测试

- 面板前端的合并与转义逻辑现在有了检查（`tests/js/panel_check.mjs`，没装 node 则跳过）：
  页面上的 JS 之前没有任何东西跑过它。检查覆盖三个渲染函数的每一个插值点。
- 新增：畸形认证头、非对象 JSON、超限大小、超量事件、伪造 `local` 字段、
  停更的快照、缓存命中、写状态失败后循环继续。

## [0.9] · 文件夹桥接（M7 追加）

用户在看完 CLI 适配器后指出：真正想要的是**让一个常驻的交互式 Claude Code 会话
参与协作，人还能中途手动插话**。每条消息起一个新进程的适配器满足不了这个。

### 新增

- **常驻的交互式会话也能参与**（`adapters/bridge.py`，`bridge = true`）：
  收到的消息写成 `bridge/inbox/*.md`，你或你一直开着的 Claude Code 把回复写进
  `bridge/outbox/`。这是本项目起点那个土办法的正式版本 —— 只不过现在它有信封、
  有回执、有 thread、有跨机传输。
  由此白捡一个能力：outbox 里放一个带 `to:` 的文件就是**主动发起**一条消息，
  所以人能随时插进正在进行的协作里说一句。配套 `anthill bridge <agent>`。

它与其他适配器的根本区别是**不阻塞**：`handle()` 只写文件就返回，回复由 runtime 的
定时 tick 扫 outbox 发出，所以人可以想十分钟，期间照常收新消息（几条一起躺在
inbox 里等）。回执仍然立即发，发起方不会以为消息丢了。
「写完了」用 mtime 判定（至少 1 秒前）—— 编辑器与 Agent 都是边写边刷盘的，
抢在中途读走会发出半句话；这比要求人写个结束标记更不容易出错。

### 修复

- `anthill send` 默认取正文前 60 字当标题，桥接渲染时把标题和正文拼在一起，
  同一句话被读了两遍。
- 桥接目录改为 agentd 启动时就建好（此前要等第一条消息到达），
  `anthill agent list` 的「大脑」一列不再把桥接 Agent 显示成 `echo` ——
  「在跑什么」是排查时第一眼看的东西，不能误导。

## [0.8] · M7 接入已有终端、Agent 对话、面板可写

### 新增

- **把 Claude Code 这类终端接进来**（`adapters/cli_agent.py`）：它们都是
  「给一段 prompt、吐一段结果」的命令行程序，配 `command = ["claude", "-p"]` 即可。
  对 runtime 只是又一个 handler。刻意与 LLM handler 保持一致：来件同样包不可信
  定界块、同样按 thread 落盘历史（外来 CLI 每次都是新进程，自己不记事）、
  同样超时杀进程组。结构化交付「能给最好，不给也不影响用」。
- **Agent 之间真的能对话**（`agent/conversation.py`）：带 @ 的对话，回信发给被 @
  的那个人并把自己 @ 回去，球就在两人之间来回；终止靠按 thread 计的 `chat_turns`
  预算（默认 6），**确定性**，不依赖模型自觉说「我说完了」，也不拿 hops 当刹车。
  配套 `anthill chat <agent>`（人跟 Agent 多轮聊）与 `anthill talk <a> <b> <话题>`。
- **面板可写**（`anthill serve --panel-write`）：发起任务、给 Agent 发消息、
  在线改 node.toml（保存前用同一套 pydantic 模型校验，不合法磁盘一个字都不改，
  并留 `node.toml.bak`）。

### 修复

- **模型纯文本回复时，它自己说的话没进 thread 记忆** —— 多轮对话里 Agent 记得
  别人说过什么，却不记得自己说过什么，会变成单方面复读。
- **coordinator 在「结果已发出、状态还没存」之间被 Ctrl-C，重启后会再给用户发一遍
  最终结果。** 改成先落盘再发送 —— 和 Sender 同一条原则。

### 安全

- 写权限 ≈ 在这台机器上执行命令（能改配置就能加一个带 `run_shell` 的 Agent），
  所以两道闸缺一不可：显式开关（默认关）+ **逐请求**校验来源是回环地址。
  `--panel-write` 配非回环地址直接拒绝启动。确认与审批仍然只在 CLI。

## [0.7] · M6 面板与打磨

### 新增

- **只读 Web 面板**（`anthill serve` → `/panel`）：拓扑（Agent 在跑没跑、积压、死信、
  对端信任状态）、任务看板（每步的状态与交付）、合并后的实时消息流。
  单页 HTML + 原生 JS + WebSocket，**无构建链、无外部资源**，没外网的服务器也能打开。
- 面板**默认只在绑回环时开启**：一旦 `--host 0.0.0.0`（为了让同网段投递进来），
  面板就会跟着暴露给整个网段，所以那种情况必须显式 `--panel`。
- 英文 README、CHANGELOG、MIT LICENSE。

### 修复

- **`httpx` 从来没被声明成依赖**（发布阻断）：`transport/lan.py` 顶层 import 它，
  而它只是被可选的 `llm` extra 顺带装进来的 —— 不装那个 extra 时整个 CLI 起不来。
- **CI 触发分支写成了 `main`，而仓库分支是 `master`**，CI 从来没跑过，
  正好掩盖了上面那条依赖问题。
- `anthill pull` / `anthill approve --peer` 把远端给的文件名与 id 直接拼进路径，
  且 `pull` 少了「收件节点必须是本机」的检查（`/deliver` 一直有，返回 421）。
  已统一要求 ULID 并补上检查。
- 版本号从 `0.1.0` 对齐到 `0.7.0`（此前与 tag 和 CHANGELOG 都对不上）。
- 终端日志里的方括号被 rich 当成样式标记吃掉（`[discovery]` 整段消失）。
- 编排恢复测试里的竞态导致偶发超时（等的条件不是单调的）。
- 面板事件流里每条日志成双出现：`serve` 的日志只有一个文件，
  但它在事件里的 agent 名是 `serve:<节点>`，照名字拼路径把同一个文件读了两遍。

### 设计说明

- 面板的数据源**全部是磁盘上的文件**，不是内存 event bus —— 一个节点上跑着好几个
  进程（每个 agentd 一个，加一个 serve），内存 bus 跨不过进程边界。
- 面板**只读**是刻意的：确认、审批、派活一律留在 CLI。一个只会 `GET` 的页面，
  最坏也就是被人看到状态，没法成为攻击面。

## [0.6] · M5 SSH 跨机

### 新增

- `transport/ssh.py`：asyncssh SFTP 直写远端邮箱，`tmp→rename` 跨机与同机语义一致；
  连接按节点复用、断了自动重连。**服务器不用开任何新端口**。
- `core/spool.py`：回信暂存区。SSH 天生单向（服务器连不回 NAT 后面的笔记本），
  所以投不出去的信封落进 `spool/<对方节点>/`，由对方 `anthill pull` 取走。
- `security/approvals.py`：远端危险操作由本机点头。走文件不走消息 ——
  消息会和串行的消费循环死锁。超时按拒绝处理。
- agentd 收件方验签：共用服务器上，同机器的其他账号也能往你的 inbox 里写文件；
  通道加密拦不住这种本地伪造投递。at-rest 验签刻意不查时间窗（邮箱是存储转发队列）。
- CLI：`anthill pull` / `approve` / `fetch`；peer 支持 `port` / `identity_file` / `known_hosts`。

### 修复

- `anthill pull` 把「连不上服务器」和「没有待取的回信」当成了同一件事，静默返回成功。

## [0.5] · M4 LAN 发现与跨机投递

### 新增

- `security/signing.py`：HMAC-SHA256 覆盖整个信封 + 5 分钟时间窗防重放。
  02-protocol §8 的协议一致性清单**至此全部打钩**。
- `discovery/`：UDP 组播信标（**默认关闭时连 socket 都不创建**）与 TOFU peers 列表。
- `web/app.py`：`/deliver` 端点，准入四道闸（400/403/401/421·404）。
- `transport/lan.py`、`anthill serve`、`anthill peers list/invite/trust/forget`。

### 修复

- **死信无限循环**：不可重试的失败没把条目移出 `outbox/pending`，
  重试循环每秒捡起来一次、每秒报一次死信。
- `invite/trust` 之后回信路由是断的：投递请求改为带 `X-AntHill-Endpoint`，
  收方验签通过后记下来。
- peers 列表被进程内缓存，改为按 mtime 自动重载。
- `peers invite` 打印的令牌被终端折行导致复制后解析失败。

## [0.4] · M3 多 Agent 编排

### 新增

- `orchestrator/`：计划 DAG（校验环与悬空依赖）、运行状态机、BOARD.md 共享黑板、
  事件驱动的 coordinator（拓扑调度、子 thread 隔离、催办与超时、`done_when` 判定与返工）。
- `send_message` 工具：worker 之间点对点 @mention，熔断由协议层的 hops 承担。
- `anthill run`：只读观察者 + rich 实时步骤表，编排逻辑全在 coordinator 里。

### 修复

- 调度器把失败的步骤当成「还没派」反复重派（每个 tick 一次，真接模型就是持续烧钱）。
- 上游失败后下游永远等不到依赖，运行永不收敛 —— 新增 `skipped` 状态。

## [0.3] · M2 Agent 大脑

### 新增

- `providers/`：`ChatProvider` 抽象 + Anthropic / OpenAI 兼容端点，`--record/--replay` 录制回放。
- `agent/tools/`：`read_file` / `write_file` / `list_dir` / `run_shell` / `finish`，
  路径逃逸防护、shell 超时杀进程组、结构化交付。
- `security/policy.py`：工具风险 × 来源信任 → 放行/确认/拒绝。
- `agent/loop.py`：ReAct 循环 + 步数与 token 双熔断；`context.py` 不可信包裹与 token 预算；
  `memory.py` thread 历史落盘与摘要压缩。

### 修复

- shell 白名单放了 `cat`/`ls`/`head`，但参数可以写绝对路径 —— `cat /etc/passwd` 照读不误。
- 熔断抛异常导致已做的工作不落盘，重试等于从头再来。
- `--yes` 的语义其实是「一律拒绝」，名字读起来却像「全部同意」→ 改名 `--unattended`。

## [0.2] · M1 传输层、agentd 与 CLI

### 新增

- `transport/`：Transport 抽象 + 同机 rename 投递。
- `agent/`：watcher（inotify，NFS 上自动降级轮询）、runtime、sender、handler 协议。
- CLI：`init` / `agent start|list` / `send` / `status` / `log --follow`。

### 修复

- 重试循环会抢发「首次投递还在路上」的消息。
- watcher 按文件名去重，导致同 ULID 重投被静默吞掉。

## [0.1] · M0 协议骨架与文件邮箱

### 新增

- `core/`：Envelope 协议、Maildir 变体邮箱与 `tmp→rename` 原子写、seen.db 幂等去重、
  投递状态机、指数退避重试与死信、路由与 @mention 解析、node.toml 与 fail-fast 体检。
- 工程化：uv + ruff + mypy strict + pre-commit + GitHub Actions。
